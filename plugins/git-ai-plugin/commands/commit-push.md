---
description: Create a conventional commit and push it, optionally opening a PR
argument-hint: "[--force|-f] [--pr]"
---

Run `/git-ai-plugin:commit --push $ARGUMENTS`. If `--pr` is present, create the pull request only after the push succeeds.
